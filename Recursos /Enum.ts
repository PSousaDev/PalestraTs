//Enumerações (enums): O TypeScript permite criar enumerações, que são tipos de dados que representam um conjunto de valores constantes.
enum Color {
  Red,
  Green,
  Blue
}

let myColor: Color = Color.Green;
